<p>Popular Tags</p>

{{#if this.isLoading}}
  <p>Loading...</p>
{{else}}
  <div class="tag-list">
    {{#each this.tags as |tag| }}
      <LinkTo @route="index" @query={{hash tag=tag}} class="tag-pill tag-default">{{tag}}</LinkTo>
    {{/each}}
  </div>
{{/if}}