{{#if this.session.isLoggedIn}}
  <CommentForm @addComment={{action this.addComment}} />
{{/if}}

{{#if this.isLoading}}
  <p>Loading comments...</p>
{{else}}
  {{#each @article.comments as |comment|}}
    <Comment @comment={{comment}} />
  {{/each}}
{{/if}}